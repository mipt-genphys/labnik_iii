% Заплатка для совместимости со старыми картинками

\usepackage{tikz}
\usepackage{epsfig}

\def\Mgot{\ensuremath{\mathfrak M}}

\def\newpic#1{%
   \def\emline##1##2##3##4##5##6{%
     \put(0,0){\tikz[remember picture,x=\unitlength,y=\unitlength,
       line width=\@wholewidth]{\draw(##1,##2)
         coordinate (em point #1 ##3) -- (##4,##5) coordinate
         (em point #1 ##6);
         \useasboundingbox (0,0) -- (em point #1
         ##3) -- (em point #1 ##6);}%
     }}}

\newpic{}

% Insert pic file
\newcommand{\oldpic}[2]{%Ширина, файл
	\begingroup
	\let\vn=\vnb
	\hbox to #1{\kern-3.5mm\input{Images/#2.pic}}%
	\endgroup
}
